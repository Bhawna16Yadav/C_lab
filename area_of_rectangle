#include <stdio.h>
void main(){
     int l,b,area;
     printf("Enter the length (l):\n);
     scanf("%d",&l);
     printf("Enter the breath (b):\n);
     scanf("%d",&b);
     area=l*b;
     printf("area of rectangle=%d",area);
}     
     
